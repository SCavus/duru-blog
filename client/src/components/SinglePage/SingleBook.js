import React, { useState } from "react";

export const SingleArt = ({ art }) => {
  return;
  <div>
    <div>
      <img src={book.url} alt="art_image" />
    </div>
    <h3>{book.title}</h3>
    <div>{book.text}</div>
  </div>;
};